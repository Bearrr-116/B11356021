# ML2025 HW4 程式碼解釋：Training Transformer（Pokémon 圖片生成）

## 作業目標

這份作業要訓練一個 **decoder-only Transformer（GPT-2 架構）**，把寶可夢圖片拆解成一串 pixel（顏色索引）組成的序列，用「next-token prediction」的方式學習如何預測下一個 pixel，最終達成生成新寶可夢圖片的效果。

整體流程：`圖片 → pixel 序列 → Transformer 自回歸訓練 → 生成新序列 → 還原成圖片`

---

## 一、環境準備（Cell 1–9）

- `pip install datasets==3.3.2`：安裝 Hugging Face 的 `datasets` 套件，用來下載資料集。
- 匯入套件：`torch`、`transformers`（`AutoModelForCausalLM`, `GPT2Config`）、`datasets`（`load_dataset`）、`PIL`（圖片處理）等。
- `!nvidia-smi`：檢查是否有 GPU 可用。
- `set_seed(0)`：固定隨機種子，確保實驗可重現。

---

## 二、資料準備（Cell 10–18）

### 1. 自訂 Dataset：`PixelSequenceDataset`

每張圖片被表示成一個「pixel 顏色索引」的序列（20×20 = 400 個 pixel）。這個類別依 `mode` 不同回傳不同內容：

| mode | 回傳內容 |
|---|---|
| `train` | `input_ids = sequence[:-1]`, `labels = sequence[1:]`（標準的「predict next token」錯位訓練法） |
| `dev` | `input_ids = sequence[:-160]`, `labels = sequence[-160:]`（用前面的 pixel 預測後面一部分，做驗證） |
| `test` | 只回傳 `input_ids`（沒有答案，等模型自己生成） |

### 2. 下載資料集並建立 DataLoader

- 從 Hugging Face Hub 下載寶可夢圖片資料集 `lca0503/ml2025-hw4-pokemon`（含 train/dev/test）。
- 下載 `lca0503/ml2025-hw4-colormap`：把每個顏色索引（token id）對應到實際 RGB 顏色的「調色盤」，`num_classes` = 調色盤大小 = 這個任務的「詞彙表大小（vocab_size）」。
- 分別包成 `train_dataloader`、`dev_dataloader`、`test_dataloader`，`batch_size=16`。

### 3. 視覺化函式

- `pixel_to_image()`：把一串顏色索引，透過 colormap 轉換回 20×20 的 RGB 圖片。
- `show_images()`：把多張圖片排成網格畫出來，方便直接檢視訓練/測試/生成的圖片長什麼樣子。

---

## 三、模型建立（Cell 19–23）

### GPT-2 Config（Cell 21）

用字典定義一個「小型 GPT-2」的架構參數，再透過 `GPT2Config.from_dict()` 轉成設定物件：

- `n_layer = 2`：只有 2 層 Transformer block（層數少，訓練快）
- `n_head = 2`：每層 2 個注意力頭
- `n_embd = 64`：embedding 維度（模型隱藏維度）較小
- `n_ctx = 128`, `n_positions = 400`：序列長度上限
- `vocab_size = num_classes`：詞彙表大小 = 顏色數量（因為這裡的「token」就是「顏色索引」，不是文字）
- `activation_function = "gelu_new"`, dropout 相關參數等

> 這裡的 hint 提到也可以把 GPT2Config 換成 `LlamaConfig`、`MistralConfig`、`Qwen2Config`，改用其他 Transformer 架構做實驗。

### 建立模型（Cell 23）

```python
model = AutoModelForCausalLM.from_config(config)
```

**關鍵重點**：這裡用的是 `from_config()`，而不是 `from_pretrained()`。也就是說模型權重是**隨機初始化**，並沒有載入任何預訓練權重，屬於「從零訓練（train from scratch）」，**沒有 fine-tuning**。

---

## 四、訓練與推論（Cell 24–33）

### 1. 訓練參數（Cell 26）

- `epochs = 50`
- `learning_rate = 1e-3`
- Loss function：`nn.CrossEntropyLoss()`（把每個 pixel 的預測當成一個分類問題：從 `num_classes` 種顏色中選一個）
- Optimizer：`AdamW`，帶 weight decay

### 2. 儲存模型函式（Cell 28）

`save_model()`：把模型權重、optimizer 狀態、epoch、loss 存成 checkpoint 檔（`.pth`），之後可以載回繼續訓練或做推論。

### 3. 訓練迴圈（Cell 30）

對每個 epoch：

1. `model.train()` 設定成訓練模式
2. 對每個 batch：
   - `input_ids`, `labels` 移到 GPU/CPU
   - `outputs = model(input_ids=input_ids).logits`：前向傳播，取得每個位置對 400 (或更少) 個顏色類別的預測分數
   - 攤平 logits 和 labels，算 `CrossEntropyLoss`
   - `loss.backward()` + `optimizer.step()`：反向傳播、更新參數
3. 計算並印出這個 epoch 的平均 loss
4. 做驗證（dev set）評估
5. 只保留 **training loss 最低**的那個 checkpoint（因為 dev accuracy 不能直接反映生成圖片的品質好壞）

### 4. 推論／生成（Cell 32）

1. 載入訓練時儲存的最佳模型權重（`best_model.pth`）
2. 對 test set 每個 batch：
   - 用 `model.generate(inputs, attention_mask=..., max_length=400)` 讓模型自回歸地一個一個 pixel 生成下去，直到湊滿 400 個 pixel（20×20 圖片）
   - 把生成結果存到 `reconstructed_results.txt`

### 5. 視覺化結果（Cell 33）

把生成的 pixel 序列透過 `pixel_to_image()` 轉回圖片，用 `show_images()` 秀出來，檢查生成的「寶可夢」長什麼樣子。

---

## 五、重點整理：這份作業有沒有用到...

| 技術 | 有沒有 | 說明 |
|---|---|---|
| **Transformer** | ✅ 有 | 核心就是 GPT-2 decoder-only Transformer（Cell 21, 23, 30, 32） |
| **Fine-tuning** | ❌ 沒有 | 模型用 `from_config()` 隨機初始化，是從零訓練（pretraining from scratch），不是載入預訓練權重去微調 |
| **RAG** | ❌ 沒有 | 沒有檢索、向量資料庫或外部知識庫 |
| **Agent** | ❌ 沒有 | 沒有工具呼叫、多步驟規劃等 agent 相關機制 |

這份作業本質上是一個「把圖片當語言建模問題做」的練習：把 pixel 顏色索引當成 token，用標準的自回歸 Transformer 訓練方式（next-token prediction）去學習生成新圖片。
